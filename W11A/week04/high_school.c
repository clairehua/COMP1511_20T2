// missing comments

#include <stdio.h>

// constants??
#define MIN 7
#define MAX 12


int main(void) {

    // variable naming
    /*int lowerLimit = 7; 
    int upperLimit = 12;*/ 
  
    printf("What year are you in? ");
  
    int userInput = 20;
    scanf("%d", &c);
    if (c > MIN || c == MIN){
        if (c < b || c == b){
            printf("You are in high school\n");
        }
    }
    if ( c < 7 || c > 12) {
        printf("You are not in high school\n");
    }
    return 0;
}
              
