// print numbers between range inputted by user
// W11A 17/6/20


#include <stdio.h>


int main(void) {
    int start, finish;
    
    printf("Enter start: ");
    scanf("%d", &start);    

    printf("Enter finish: ");
    scanf("%d", &finish);
    
    while (start <= finish) {
        printf("%d\n", start);   
        start++;
        // start += 1
        // start = start + 1
    }   

    return 0;
}
