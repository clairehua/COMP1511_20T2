#include "queue.h"
#include <stdio.h>

int main(void) {
    //TODO: Add some code that makes a queue
    
    Queue q = newQueue();
    // DON'T DO THIS!!
    // printf(q->start->data);
    
    return 0;
}
