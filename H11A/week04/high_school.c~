// missing comments!!


#include <stdio.h>

#define LOWER 7
#define UPPER 12

int main(void) {
    // variable names!!
    // what is a..?
    // lowerLimit, upperLimit, userYear
    int a = 7; int b = 12; int c;
    printf("What year are you in? ");
    c = 20;
    scanf("%d", &c);
    // c >= a
    // &&
    if (c > a || c == a){
        if (c < b || c == b){
            printf("You are in high school\n");
        }
    }
    // replace this with else
    if ( c < 7 || c > 12) {
        printf("You are not in high school\n");
    }
    return 0;
}
